# VPN Rotator Configuration
# Format: key=value

# Maximum requests per region before forced rotation
max_requests_per_region=25

# Minimum time between rotations
min_rotation_interval=5s

# Enable automatic rotation on 429 status code
detect_on_429=true

# Enable predictive rotation based on learned limits
predictive=true

# Fall back to proxies.txt if vpn.txt is missing
fallback_to_proxies=true

# Maximum 429 errors before immediate rotation
max_ratelimit_hits=2

# Percentage of predicted limit before rotation (80 = rotate at 80%)
predictive_threshold=80